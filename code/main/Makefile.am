#Makefile for the auto make generating input for the configure.ac

bin_PROGRAMS = maze_game #generated program
maze_game_SOURCES = main.c input.c game.c draw.c
AM_CFLAGS = `pkg-config --cflags gtk+-3.0`
